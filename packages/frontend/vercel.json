{
  "framework": "vite",
  "buildCommand": "cd ../.. && npm run build:shared && npm run build:frontend",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
